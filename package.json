{
  "name": "alfarajia-platform-android",
  "version": "1.0.0",
  "private": true,
  "description": "منصة ثانوية الفراجية للبنين - Android wrapper with durable offline sync",
  "scripts": {
    "android:init": "npx cap add android && node scripts/prepare-android.mjs && npx cap sync android",
    "android:sync": "node scripts/prepare-android.mjs && npx cap sync android",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "7.4.4",
    "@capacitor/android": "7.4.4"
  },
  "devDependencies": {
    "@capacitor/cli": "7.4.4"
  }
}
